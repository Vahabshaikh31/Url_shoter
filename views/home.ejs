<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        /* Basic reset */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
    font-family: 'Roboto', sans-serif;
    color: #ffffff;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    overflow: hidden;
}

h1 {
    font-size: 3rem;
    color: #00e6e6;
    text-align: center;
    margin-bottom: 20px;
    text-transform: uppercase;
    letter-spacing: 2px;
    text-shadow: 0 0 15px #00e6e6, 0 0 20px #00e6e6, 0 0 30px #00e6e6;
}

div {
    background: rgba(255, 255, 255, 0.1);
    padding: 30px;
    border-radius: 10px;
    box-shadow: 0 0 15px rgba(0, 230, 230, 0.5), 0 0 25px rgba(0, 230, 230, 0.3);
    width: 400px;
    text-align: center;
}

p {
    font-size: 1.2rem;
    margin-bottom: 20px;
    background: rgba(0, 230, 230, 0.1);
    padding: 10px;
    border-radius: 5px;
    text-shadow: 0 0 10px #00e6e6, 0 0 15px #00e6e6;
}

label {
    font-size: 1.2rem;
    color: #00e6e6;
    display: block;
    margin-bottom: 10px;
    text-shadow: 0 0 10px #00e6e6, 0 0 15px #00e6e6;
}

input[type="text"] {
    width: 100%;
    padding: 10px;
    border: none;
    border-radius: 5px;
    background: rgba(255, 255, 255, 0.2);
    color: #ffffff;
    font-size: 1rem;
    margin-bottom: 20px;
    box-shadow: 0 0 15px rgba(255, 255, 255, 0.1);
    text-shadow: 0 0 10px #00e6e6, 0 0 15px #00e6e6;
}

input[type="text"]::placeholder {
    color: rgba(255, 255, 255, 0.7);
}

button {
    width: 100%;
    padding: 10px;
    background: #00e6e6;
    border: none;
    border-radius: 5px;
    color: #0f2027;
    font-size: 1.2rem;
    cursor: pointer;
    transition: background 0.3s ease;
    text-transform: uppercase;
    box-shadow: 0 0 15px #00e6e6, 0 0 20px #00e6e6, 0 0 30px #00e6e6;
}

button:hover {
    background: #00cccc;
}

    </style>
</head>
<body>
    <h1>URL Shortener</h1>
    <div>
        <% if(locals.id) {%>
             <p>url Genrated : http://localhost:8000/<%= id %> </p>
        <% } %>
       
        <form method="POST" action="/api/url">
            <label for="">Enter Original URL </label>
            <input type="text" name="url" placeholder="www.example.com">
            <button type="submit">Generate</button>
        </form>
    </div>
</body>
</html>